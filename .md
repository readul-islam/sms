saas-survey-platform/
│── apps/ # Main applications
│ ├── Frontend/ # Next.js + React + TypeScript
│ │ ├── public/ # Static assets
│ │ ├── src/
│ │ │ ├── pages/ # Next.js routes
│ │ │ ├── components/ # Reusable UI components
│ │ │ ├── modules/ # Feature-specific UI (Survey, Analytics, Auth)
│ │ │ ├── hooks/ # Custom React hooks
│ │ │ ├── store/ # State management (Zustand/Redux)
│ │ │ ├── utils/ # Helper functions
│ │ │ └── styles/ # Global + module CSS/Tailwind
│ │ └── tsconfig.json
│ │
│ ├── Backend/ # NestJS + TypeScript
│ │ ├── src/
│ │ │ ├── main.ts # App entry
│ │ │ ├── app.module.ts # Root module
│ │ │ ├── common/ # Shared pipes, guards, interceptors, filters
│ │ │ ├── config/ # Env configs (DB, Redis, Stripe, etc.)
│ │ │ ├── modules/
│ │ │ │ ├── auth/ # AuthModule (JWT, OAuth, RBAC)
│ │ │ │ ├── users/ # UserModule (CRUD, profiles, roles)
│ │ │ │ ├── surveys/ # SurveyModule (CRUD surveys)
│ │ │ │ ├── responses/ # ResponseModule (collect responses)
│ │ │ │ ├── analytics/ # AnalyticsModule (reports, stats)
│ │ │ │ ├── billing/ # BillingModule (Stripe, plans)
│ │ │ │ ├── notify/ # NotificationModule (email, WebSockets)
│ │ │ │ ├── webhook/ # WebhookModule (Slack, Zapier)
│ │ │ │ └── gateway/ # GatewayModule (WebSockets real-time)
│ │ │ ├── prisma/ # Prisma schema for PostgreSQL
│ │ │ ├── schemas/ # Mongoose schemas for MongoDB
│ │ │ ├── dto/ # DTOs (shared request/response types)
│ │ │ └── utils/ # Utility functions
│ │ ├── test/ # Unit + E2E tests
│ │ └── tsconfig.json
│
│── packages/ # Shared libraries
│ ├── ui/ # Shared UI components (React design system)
│ ├── types/ # Shared TypeScript types/interfaces
│ ├── utils/ # Shared utilities (date, string helpers, etc.)
│ └── config/ # Shared ESLint, Prettier, Tailwind configs
│
│── infra/ # DevOps + Deployment
│ ├── docker/ # Dockerfiles for frontend, backend, db
│ ├── k8s/ # Kubernetes manifests
│ ├── ci-cd/ # GitHub Actions workflows
│ └── nginx/ # Reverse proxy configs
│
│── .env.example # Example environment variables
│── docker-compose.yml # Local dev setup
│── package.json
│── turbo.json # Turborepo config (if using)
│── nx.json # Nx config (if using Nx)
│── tsconfig.json
│── README.md
