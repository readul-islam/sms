'use client';

// import '@SMS/styles';
import { ButtonExamples, TooltipExamples } from '@SMS/ui';
export default function Index() {
  /*
   * Replace the elements below with your own.
   *
   * Note: The corresponding styles are in the ./index.tailwind file.
   */
  return (
    <div className="p-10">
      <TooltipExamples />
      <ButtonExamples />
    </div>
  );
}
